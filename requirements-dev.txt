# Development-only dependencies (tests, linting, security scanning).
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0.0            # unit-test runner for the assertion tests in tests/
ruff>=0.5.0              # fast linter + formatter (replaces flake8/isort/black)
cdk-nag>=2.28.0          # scans the synthesized template against AWS best practices
boto3>=1.34.0            # used by scripts/generate_presigned_url.py
